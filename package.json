{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt generate && aws s3 sync .output/public s3://inbox.aazvf.art/ --profile s3deploy --delete"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.5.0",
    "autoprefixer": "^10.4.12",
    "nuxt": "3.0.0-rc.11",
    "postcss": "^8.4.16",
    "tailwindcss": "^3.1.8"
  },
  "dependencies": {
    "localforage": "^1.10.0"
  }
}
